#include "assert.h"
#include "stdlib.h"
#include "stdio.h"
#include "string.h"

#define fore(i, s, e) for(u32 i = s; i < (u32)e; i++)
#define max(a, b) ((a > b) ? a : b)
#define min(a, b) ((a > b) ? b : a)